swagger: "2.0"

info:
  title: swagger1st TODO list
  version: "1.0"

paths:
  /todos:
    get:
      summary: get all TODO entries
      description: Returns a list of all TODO entries.
      operationId: todo.api/get-todo-entries
      produces:
        - application/json
      responses:
        200:
          description: all TODO entries
